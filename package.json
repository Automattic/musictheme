{
  "name": "musictheme",
  "version": "1.0.0",
  "repository": "https://github.com/automattic/musictheme/",
  "license": "GPL-2.0-or-later",
  "description": "",
  "devDependencies": {
    "node-sass": "^4.9.3"
  },
  "scripts": {
    "watch": "node-sass sass -o . --output-style expanded && node-sass --watch sass -o . --output-style expanded",
    "build": "node-sass sass -o . --output-style expanded"
  }
}
